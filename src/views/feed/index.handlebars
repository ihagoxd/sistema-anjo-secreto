<div class="mural-layout">
  <div class="mural-centro">

    <div class="stories">
      <div class="story">
        <div class="story-ring-area">
          <a class="story-ring eu" href="/u/{{usuario.usuario}}" title="Seu perfil"><span class="story-ring-in">{{#if usuario.foto_perfil}}<img src="{{usuario.foto_perfil}}" alt="">{{else}}{{inicial usuario.nome}}{{/if}}</span></a>
          <button type="button" class="story-badge {{#unless humorEu}}editavel{{/unless}}" data-abrir-modal="modal-humor" title="{{#if humorEu}}{{rotuloHumor humorEu}} · alterar status{{else}}Definir seu status de hoje{{/if}}" aria-label="Definir status do dia">
            {{#if humorEu}}{{emojiHumor humorEu}}{{else}}<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4z"/></svg>{{/if}}
          </button>
        </div>
        <span class="story-nome">Você</span>
      </div>
      {{#each equipe}}
        <a class="story" href="/u/{{this.usuario}}" title="{{this.nome}}{{#if this.humor}} · {{rotuloHumor this.humor}}{{/if}}">
          <div class="story-ring-area">
            <span class="story-ring"><span class="story-ring-in">{{#if this.foto_perfil}}<img src="{{this.foto_perfil}}" alt="">{{else}}{{inicial this.nome}}{{/if}}</span></span>
            {{#if this.humor}}<span class="story-badge">{{emojiHumor this.humor}}</span>{{/if}}
          </div>
          <span class="story-nome">{{this.usuario}}</span>
        </a>
      {{/each}}
    </div>

    <form class="post-compositor card" action="/feed" method="POST" enctype="multipart/form-data" novalidate>
      <input type="hidden" name="_csrf" value="{{csrfToken}}">
      <div class="compositor-topo">
        <span class="post-avatar">{{#if usuario.foto_perfil}}<img src="{{usuario.foto_perfil}}" alt="">{{else}}{{inicial usuario.nome}}{{/if}}</span>
        <textarea name="texto" maxlength="{{limite}}" placeholder="No que você está pensando?" autocomplete="off" data-mencoes data-texto></textarea>
      </div>
      <div class="compositor-preview" data-preview hidden></div>
      <div class="compositor-rodape">
        <div class="compositor-acoes">
          <button type="button" class="comp-acao" data-emoji aria-label="Emoji" title="Emoji">😊</button>
          <button type="button" class="comp-acao" data-foto-btn aria-label="Foto" title="Foto">
            <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
          </button>
          <button type="button" class="comp-acao" data-camera aria-label="Câmera" title="Câmera">
            <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
          </button>
        </div>
        <input type="file" name="imagem" accept="image/png,image/jpeg,image/gif,image/webp" class="visualmente-oculto" data-foto>
        <input type="file" name="video" accept="video/webm,video/mp4,video/quicktime" class="visualmente-oculto" data-video tabindex="-1">
        <button type="submit" class="btn btn-primario">Publicar</button>
      </div>
    </form>

    <div class="feed">
      {{#each posts}}
        {{> postCard}}
      {{else}}
        <div class="card card-centro"><p class="texto-suave" style="margin:0;">Nenhuma publicação ainda. Seja o primeiro a postar.</p></div>
      {{/each}}
    </div>
  </div>

  <aside class="mural-lado">
    <a class="lado-eu" href="/u/{{usuario.usuario}}">
      <span class="lado-eu-av">{{#if usuario.foto_perfil}}<img src="{{usuario.foto_perfil}}" alt="">{{else}}{{inicial usuario.nome}}{{/if}}</span>
      <div class="lado-eu-txt">
        <div class="lado-eu-user">{{usuario.usuario}}</div>
        <div class="lado-eu-nome">{{usuario.nome}}</div>
      </div>
      <span class="lado-eu-editar">Ver perfil</span>
    </a>

    {{#if sugestoes.length}}
      <div class="lado-titulo">Equipe</div>
      {{#each sugestoes}}
        <a class="sug" href="/u/{{this.usuario}}">
          <span class="sug-av">{{#if this.foto_perfil}}<img src="{{this.foto_perfil}}" alt="">{{else}}{{inicial this.nome}}{{/if}}</span>
          <div class="sug-txt">
            <div class="sug-nome">{{this.nome}}</div>
            <div class="sug-user">@{{this.usuario}}</div>
          </div>
          <span class="sug-ver">Ver</span>
        </a>
      {{/each}}
    {{/if}}

    <p class="lado-rodape">Anjo Secreto · {{anoAtual}}</p>
  </aside>
</div>

<div class="modal sheet-share" id="modal-share" hidden aria-hidden="true">
  <div class="modal-card share-card">
    <div class="sheet-cab sempre">
      <span class="sheet-grab" aria-hidden="true"></span>
      <span class="sheet-titulo">Compartilhar</span>
      <button type="button" class="sheet-x" data-fechar-modal="modal-share" aria-label="Fechar">&times;</button>
    </div>
    <div class="share-lista">
      {{#each equipe}}
        <button type="button" class="share-item" data-share-dm="{{this.id_usuario}}">
          <span class="share-av">{{#if this.foto_perfil}}<img src="{{this.foto_perfil}}" alt="">{{else}}{{inicial this.nome}}{{/if}}</span>
          <span class="share-txt">
            <span class="share-nome">{{this.nome}}</span>
            <span class="share-user">@{{this.usuario}}</span>
          </span>
          <span class="share-envia">Enviar</span>
        </button>
      {{else}}
        <div class="texto-suave" style="padding:10px 6px;">Ninguém disponível no Direct ainda.</div>
      {{/each}}
    </div>
    <button type="button" class="share-item share-copiar" data-share-copiar>
      <span class="share-av share-av-link" aria-hidden="true">
        <svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
      </span>
      <span class="share-txt"><span class="share-nome">Copiar link</span></span>
    </button>
  </div>
</div>

<div class="modal" id="modal-humor" hidden aria-hidden="true">
  <div class="modal-card modal-humor">
    <div class="modal-cab">
      <h2>Como você está hoje?</h2>
      <p class="texto-suave">Seu status aparece no seu story para a equipe e some à meia-noite.</p>
    </div>
    <form action="/conta/humor" method="POST" class="humor-grade">
      <input type="hidden" name="_csrf" value="{{csrfToken}}">
      {{#each humores}}
        <button type="submit" name="humor" value="{{this.chave}}" class="humor-op {{#if (eq this.chave @root.humorEu)}}sel{{/if}}">
          <span class="humor-emoji">{{this.emoji}}</span>
          <span class="humor-rotulo">{{this.rotulo}}</span>
        </button>
      {{/each}}
    </form>
    <div class="modal-acoes">
      {{#if humorEu}}
        <form action="/conta/humor" method="POST" style="margin:0;">
          <input type="hidden" name="_csrf" value="{{csrfToken}}">
          <input type="hidden" name="humor" value="">
          <button type="submit" class="btn btn-secundario btn-sm">Remover status</button>
        </form>
      {{/if}}
      <button type="button" class="btn btn-secundario btn-sm" data-fechar-modal="modal-humor">Fechar</button>
    </div>
  </div>
</div>
